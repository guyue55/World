import { contentChannels, contentIndexEntries } from '@/features/_legacy/content-taxonomy'
export function ContentChannelBoard(){return <section className="grid gap-4 md:grid-cols-3">{contentChannels.map(channel=><article key={channel.id} className="rounded-[2rem] border border-white/50 bg-white/75 p-5 shadow-soft"><p className="text-xs tracking-[0.3em] text-moss">{channel.id.toUpperCase()}</p><h3 className="mt-3 text-2xl font-semibold">{channel.title}</h3><p className="mt-3 text-sm leading-6 text-ink/65">{channel.description}</p><p className="mt-4 text-xs text-ink/45">placements: {channel.placements.join(' / ')} · entries: {contentIndexEntries.filter(entry=>entry.channel===channel.id).length}</p></article>)}</section>}
