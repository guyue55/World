import Link from 'next/link'
import { contentNodes, readingPaths } from '@/features/_legacy/content-constellation'
export function ReadingPathView(){ return <section className="space-y-4">{readingPaths.map((path)=><article key={path.id} className="rounded-[2rem] border border-white/50 bg-white/75 p-5 shadow-soft"><p className="text-xs tracking-[0.32em] text-moss">READING PATH</p><h3 className="mt-3 text-2xl font-semibold">{path.title}</h3><p className="mt-3 text-sm leading-6 text-ink/65">{path.description}</p><div className="mt-4 flex flex-wrap gap-2">{path.nodeIds.map((nodeId)=>{ const node=contentNodes.find((item)=>item.id===nodeId); return node ? <Link key={node.id} href={node.href} className="rounded-full bg-ink px-4 py-2 text-xs text-white">{node.title}</Link> : null })}</div></article>)}</section> }
