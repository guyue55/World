import Link from 'next/link'
import { contentEdges, contentNodes } from '@/features/_legacy/content-constellation'
export function ContentStarMap(){ return <section className="rounded-[3rem] border border-white/20 bg-ink p-6 text-white shadow-soft"><p className="text-xs tracking-[0.34em] text-white/50">CONTENT STAR MAP</p><div className="mt-6 grid gap-4 md:grid-cols-3">{contentNodes.map((node)=><Link key={node.id} href={node.href} className="rounded-[2rem] border border-white/15 bg-white/10 p-5 transition hover:bg-white/15"><p className="text-xs tracking-[0.26em] text-white/45">{node.type.toUpperCase()}</p><h3 className="mt-3 text-xl font-semibold">{node.title}</h3><p className="mt-3 text-sm leading-6 text-white/60">{node.description}</p><p className="mt-4 text-xs text-white/45">weight {node.weight} · {node.visibility}</p></Link>)}</div><p className="mt-5 text-sm text-white/50">关系边：{contentEdges.length} 条，私密节点只显示脱敏状态。</p></section> }
