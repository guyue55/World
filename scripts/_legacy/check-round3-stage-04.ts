import { spawnSync } from 'node:child_process'; for(const n of ['13','14','15','16']){const r=spawnSync('npm',['run',`check:round3:${n}`],{stdio:'inherit',shell:process.platform==='win32'}); if(r.status!==0) process.exit(r.status??1)} console.log('Round 03 stage 04 checks passed.')
