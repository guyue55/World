# 古月浮屿 V1 完整项目包说明

> 打包时间：2026-05-19
> 类型：完整项目包（当前可用 delta 合并版）

## 合并情况

```text
已合并 delta：35 / 36
缺失 delta：1
项目文件数：497
JSON 文件数：116
JSON 解析错误：0
```

## 缺失说明

当前环境中缺失以下 delta 包：

```text
古月浮屿_V1第十九批治理台账打磨_delta.zip
```

其中此前审查已发现：第十九批治理台账相关文档在当前合并结果中缺失，主要影响：

```text
docs/05-engineering/v1-governance-ledger.md
docs/05-engineering/v1-risk-register.md
docs/05-engineering/v1-decision-traceability.md
docs/05-engineering/v1-capability-maturity-model.md
```

## 第一阶段状态

```text
stage-completion-gate.currentStatus = not-yet-complete
phase-one-final-decision.currentDecision = not-yet-complete
```

当前不能宣布第一阶段完成。需要在真实项目目录执行：

```bash
npm install
npm run lint
npm run typecheck
npm run check:world-core
npm run build
npm run acceptance:local
GUYUE_PREVIEW_URL=https://your-preview-url.example npm run preview:smoke
npm run closure:final
```

只有 `closure:final` 输出 `complete` 后，才执行：

```bash
npm run stage:complete
```

## lint 状态

配置层面：

```text
lint = eslint . --max-warnings=0
```

但本打包环境没有安装 node_modules，因此没有执行真实 `npm run lint`。真实 lint 结果必须以项目目录执行为准。

## 包内清单

```text
FULL_PROJECT_PACKAGE_MANIFEST.json
FULL_PROJECT_PACKAGE_NOTE.md
```

## 下载文件

```text
古月浮屿_V1完整项目包_当前合并版.zip
```
