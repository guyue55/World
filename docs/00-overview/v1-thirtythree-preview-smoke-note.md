# 古月浮屿｜V1 第三十三批预览部署冒烟检查说明

> 本文件说明第三十三批变更：把预览部署冒烟检查、公开端点检查、隐私 token 检查和性能实测运行手册纳入第一阶段收口。

## 本次重点

```text
预览部署冒烟检查配置
预览部署记录模板
性能实测运行手册
预览冒烟脚本
公开端点隐私 token 检查
```

## 新增命令

```bash
npm run check:preview-smoke-config
npm run preview:smoke
npm run preview:print
```

## 阶段判断

第一阶段仍不能宣布完成。此批补齐的是预览部署后的真实环境检查入口。

## 核心判断

```text
预览发布不是上线，
而是让真实环境暴露骨架问题。
```
