# 服务端权限边界说明

前端显隐只能改善体验，不能承担安全职责。

因此：

- 私密、家庭、保险箱和创世台内容不得进入公开索引。
- 旧阶段入口必须由 middleware 或服务端页面重定向/阻断。
- `robots.txt` 与 `sitemap.ts` 必须和 route policy 一致。
- 公开 AI 只能读取公开上下文。
- 任何危险动作都必须有服务端边界和审计记录。

这条规则保护世界的公共前厅，也保护还没有公开的内室。
